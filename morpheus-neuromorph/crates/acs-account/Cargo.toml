[package]
name = "acs-account"
version = "0.1.0"
edition = "2021"
description = "Augmented-Citizen Sovereign Account interfaces and guards."
license = "MIT"
repository = "https://github.com/your-user/morpheus-neuromorph"

[dependencies]
serde = { version = "1", features = ["derive"] }
